■ 概要
ホスト名（コンピュータ名）を変更します。
hostlist.csv で選択したホストの NewPCName が適用されます。

■ 設定ファイル
設定CSVはありません。
hostlist.csv の NewPCName 列の値が使用されます。

■ 前提条件
・Fabriq起動時にホストを選択済みであること

■ 注意事項
・変更の反映にはOSの再起動が必要です
・既に同じホスト名の場合は自動的にスキップされます

■ 動作の流れ
1. 現在のホスト名と変更先を表示
2. 実行確認（Y/N）
3. Rename-Computer でホスト名を変更
